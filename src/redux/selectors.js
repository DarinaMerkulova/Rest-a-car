export const selectCars = state => state.car.adverts;

export const selectLoading = state => state.car.isLoading;
export const selectError = state => state.car.error;

// export const selectFilter = state => state.filter.filter;


